apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: w7x6t-dev-credentials
spec:
  secretStoreRef:
    name: vault
    kind: ClusterSecretStore
  refreshInterval: 5m
  data:
  - secretKey: username
    remoteRef:
      key: kv/services/argocd/repo-creds-w7x6t-dev
      property: username
  - secretKey: password
    remoteRef:
      key: kv/services/argocd/repo-creds-w7x6t-dev
      property: password
  target:
    deletionPolicy: Delete
    template:
      engineVersion: v2
      metadata:
        labels:
          argocd.argoproj.io/secret-type: repo-creds
      data:
        type: git
        url: https://scm.w7x6t.dev/
        username: '{{ .username }}'
        password: '{{ .password }}'

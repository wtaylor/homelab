apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: sparky-fitness-postgres-app-credentials
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: op-homelab
  refreshInterval: 5m
  data:
  - secretKey: username
    remoteRef:
      key: sparky-fitness/postgres-app/username
  - secretKey: password
    remoteRef:
      key: sparky-fitness/postgres-app/password

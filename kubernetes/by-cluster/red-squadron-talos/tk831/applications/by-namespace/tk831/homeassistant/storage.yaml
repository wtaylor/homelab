apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: homeassistant-data-volsync-restic-config
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: op-homelab
  refreshInterval: 60m
  target:
    template:
      type: kubernetes.io/Opaque
      data:
        RESTIC_REPOSITORY: rest:http://data-backup-server.willtaylor.info:8080/volumes/homeassistant-data
        RESTIC_PASSWORD: "{{ .restic_password }}"
        RESTIC_REST_USERNAME: "{{ .rclone_username }}"
        RESTIC_REST_PASSWORD: "{{ .rclone_password }}"
    deletionPolicy: Delete
  data:
  - secretKey: rclone_username
    remoteRef:
      key: volsync/rclone/username
  - secretKey: rclone_password
    remoteRef:
      key: volsync/rclone/password
  - secretKey: restic_password
    remoteRef:
      key: volsync/restic/password
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: homeassistant-data
  labels:
    app.kubernetes.io/name: homeassistant
spec:
  storageClassName: proxmox-zfs
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 32Gi
---
apiVersion: volsync.backube/v1alpha1
kind: ReplicationSource
metadata:
  name: homeassistant-data
spec:
  sourcePVC: homeassistant-data
  trigger:
    # Take a backup every night at 4am
    schedule: "00 4 * * *"
  restic:
    cacheStorageClassName: local-path-ssd
    copyMethod: Direct
    pruneIntervalDays: 7
    repository: homeassistant-data-volsync-restic-config
    retain:
      hourly: 1
      daily: 7
      weekly: 2
      monthly: 2
      yearly: 0

apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: postgres-tk831-default-role-sparkyfitness-admin
spec:
  secretStoreRef:
    name: vault
    kind: ClusterSecretStore
  refreshInterval: 5m
  target:
    template:
      type: kubernetes.io/basic-auth
    deletionPolicy: Delete
  data:
  - secretKey: username
    remoteRef:
      key: kv/services/sparky-fitness/postgres-credentials
      property: username
  - secretKey: password
    remoteRef:
      key: kv/services/sparky-fitness/postgres-credentials
      property: password
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: postgres-tk831-default-role-sparkyfitness-app
spec:
  secretStoreRef:
    name: vault
    kind: ClusterSecretStore
  refreshInterval: 5m
  target:
    template:
      type: kubernetes.io/basic-auth
    deletionPolicy: Delete
  data:
  - secretKey: username
    remoteRef:
      key: kv/services/sparky-fitness/postgres-app-credentials
      property: username
  - secretKey: password
    remoteRef:
      key: kv/services/sparky-fitness/postgres-app-credentials
      property: password

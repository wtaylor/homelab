apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: postgres-default-admin-credentials
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: op-homelab
  refreshInterval: 5m
  data:
  - secretKey: username
    remoteRef:
      key: postgres/dbc-postgres-default-credentials/superuserUsername
  - secretKey: password
    remoteRef:
      key: postgres/dbc-postgres-default-credentials/superuserPassword
  target:
    deletionPolicy: Delete
    template:
      engineVersion: v2
      type: kubernetes.io/basic-auth
      data:
        username: '{{ .username }}'
        password: '{{ .password }}'
